alert('hola UDO');
function login()
{
    //login
    var username = document.getElementById('txtidusuario').value;
    var password = document.getElementById('txtPasswd').value;
    var recorder = document.getElementById('remember').value;
    alert('Usuario'+ username +', Cointrasena'+ password+', rcordar'+ recorder);
}